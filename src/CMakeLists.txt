set (connected_components_VERSION_MAJOR 0)
set (connected_components_VERSION_MINOR 1)

include_directories("${connected_components_PROJECT_DIR}/include")

add_executable (connected_components main.cpp)
